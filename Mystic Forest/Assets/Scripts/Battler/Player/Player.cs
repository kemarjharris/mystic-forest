using UnityEngine;
using System.Collections;

public class Player : Battler, IPlayer
{
    /*
    public GameObject[] observers;

    private void Start()
    {
        for (int i = 0; i < observers.Length; i++)
        {
            observers[i].GetComponent<MonoObserver<IPlayer>>().Observe(this);
        }
    }
    */
}
